# Global working agreements

## GitHub

- GitHub account: `xiaobaofamily`.
- Git author: `Zhenwei Dai <spchovy@Gmail.com>`.
- Codex may create local Git commits after completing and verifying requested changes.
- Do not push commits to GitHub; Zhenwei will run `git push`.
- When giving Zhenwei push instructions, always provide the explicit command to `cd` into this repository first, followed by the `git push` command.
- For this repository, use `cd "/Users/zhenweidai/Documents/storage- management"`.
- Never store GitHub passwords, personal access tokens, or other credentials in repository files or global instructions.
